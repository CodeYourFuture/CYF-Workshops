// Ensure you're in the arrays directory of this repo
// Log the return value of fs.readdirSync below, then store it in an appropriately named variable
// Now iterate and print out only the js files in the array
const fs = require("fs");

fs.readdirSync("./");
